# 모바일 앱 테스터의 눈: ADB와 Xcode 로그로 시스템 크래시 원인 분석하기

화면이 갑자기 꺼지거나 "앱을 중단하였습니다"라는 팝업이 뜰 때, 단순히 "앱이 죽어요"라고 보고하면 개발자는 원인을 찾기 위해 한참을 헤매게 됩니다. 이때 **로그캣(Logcat)**을 이용해 결정적 증거를 확보하는 방법입니다.

## 🛠️ 모바일 테스터의 필수 무기: ADB로 크래시 로그 뽑아내기

### 1. 준비 단계: 기기와 PC 연결

- **개발자 옵션 활성화**: 안드로이드 설정 > 휴대전화 정보 > 빌드 번호를 7번 연타합니다.

- **USB 디버깅 허용**: 개발자 옵션 메뉴에서 'USB 디버깅'을 켭니다.

- **연결 확인**: PC 터미널(CMD)에서 adb devices를 입력했을 때 기기 ID가 나오면 성공입니다.

### 2. 실전 로그 추출: Logcat 활용법

터미널에 다음 명령어를 입력하면 실시간으로 기기 내부에서 일어나는 모든 기록이 쏟아집니다.

- **명령어**: adb logcat *:E

- **\*:E 옵션**: 오직 Error(E) 등급의 로그만 필터링해서 보여줍니다. 무수히 많은 정보 속에서 쓰레기값을 걸러내는 핵심 팁입니다.

### 3. "범인 검거" - Stack Trace 읽는 법

로그가 멈추거나 붉은색 글씨가 보인다면 그곳에 범인이 있습니다. 테스터는 다음 키워드를 찾아야 합니다.

- **FATAL EXCEPTION**: 앱이 죽은 직접적인 이유입니다.

- **NullPointerException**: 데이터가 있어야 할 곳에 아무것도 없어서 터진 경우입니다. (가장 흔함)

- **OutOfMemoryError**: 이미지가 너무 크거나 메모리 관리가 안 되어 앱이 버티지 못한 경우입니다.

- **Caused by**: 에러가 시작된 '진짜 원인'이 적힌 줄입니다.

### 4. 실무 꿀팁: 파일로 저장해서 전달하기

로그가 너무 빨리 지나가서 보기 힘들다면 파일로 저장해 개발자에게 슬랙(Slack)이나 지라(Jira)로 던져주세요.

- **명령어**: adb logcat -d > crash_log.txt

이 명령어를 치면 현재까지 쌓인 로그가 crash_log.txt 파일로 저장됩니다.

## 📝 개발자에게 환영받는 '크래시 결함' 보고서 예시

### [결함 요약] 
프로필 사진 변경 후 '저장' 클릭 시 앱 강제 종료

### [재현 경로] 
설정 > 프로필 편집 > 갤러리에서 이미지 선택 > 저장 버튼 클릭

### [확인된 로그(Logcat)]

```
E/AndroidRuntime: FATAL EXCEPTION: main
Process: com.test.app, PID: 12345
java.lang.NullPointerException: Attempt to invoke virtual method 'String android.net.Uri.getPath()' on a null object reference
at com.test.app.ProfileActivity.saveData(ProfileActivity.java:85)
```

### [의견] 
이미지 경로(URI)를 가져오는 과정에서 Null 값이 발생하여 Crash가 나는 것으로 보입니다.

## 💡 테스터를 위한 한 줄 요약

"로그는 테스터의 알리바이입니다." 내가 발견한 현상이 기분 탓이 아니라 시스템의 결함임을 증명하는 가장 강력한 수단이죠.