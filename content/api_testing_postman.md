# API 테스트 실무: Postman으로 백엔드 로직 먼저 두들겨 보기

UI 화면(Frontend)이 완성될 때까지 기다리는 테스터는 배포 직전의 일정 압박에 시달릴 수밖에 없습니다. 하지만 API 테스트를 할 줄 알면, 화면이 나오기도 전에 백엔드 로직의 결함을 70% 이상 먼저 잡아낼 수 있습니다.

가장 대중적인 도구인 Postman을 활용해 효율적으로 API를 검증하는 실무 가이드를 정리해 드립니다.

## 📂 [테스트 수행] API 테스트 실무: Postman 활용 가이드

### 1. 왜 API 테스트를 먼저 해야 하는가?

- **빠른 피드백**: UI 개발을 기다리지 않고 서버의 비즈니스 로직을 즉시 검증할 수 있습니다.

- **에러 위치 파악**: 화면에서 에러가 날 때, 이게 프론트엔드의 화면 표시 문제인지 백엔드의 데이터 문제인지 명확히 알 수 있습니다.

- **보안 및 예외 케이스 검증**: UI에서 막아둔 제한(예: 마이너스 수량 입력 방지)를 우회하여 서버가 잘못된 데이터를 받아들이는지 체크할 수 있습니다.

### 2. Postman 기본 구성과 요청 보내기

API 테스트의 핵심은 **"서버에 무엇을 던지고(Request), 무엇을 받는가(Response)"**입니다.

- **Method**: GET(조회), POST(생성), PUT(수정), DELETE(삭제)를 목적에 맞게 선택합니다.

- **URL**: 테스트할 서버의 주소를 입력합니다. (예: https://api.test.com/v1/orders)

- **Params & Body**: 
  - Params: URL 뒤에 붙는 검색 조건 (예: ?item_id=10)
  - Body: 서버로 보낼 실제 데이터. 주로 raw > JSON 형식을 사용합니다.

- **Headers**: 인증 토큰(Authorization)이나 데이터 타입(Content-Type)을 설정합니다.

### 3. API 테스트에서 반드시 체크해야 할 4가지

#### ① 상태 코드 (Status Code)

200 OK 혹은 201 Created가 오는지 확인합니다.

실패해야 하는 상황(잘못된 값 입력)에서는 400 Bad Request나 403 Forbidden이 오는지 확인합니다.

#### ② 응답 본문 (Response Body)

데이터의 필드명이 기획서와 일치하는지 확인합니다. (예: userName인지 user_name인지)

데이터 타입이 올바른지 확인합니다. (숫자여야 할 가격이 문자열 "1000"으로 오지 않는지)

#### ③ 필수 값(Required) 및 유효성 검사

필수 파라미터를 빼고 보냈을 때 서버가 적절한 에러 메시지를 주는지 확인합니다.

가격에 -100을 넣거나, 날짜에 9999-99-99를 넣었을 때 서버가 걸러내는지 확인합니다.

#### ④ 응답 속도 (Time)

API 응답이 너무 느리지 않은지 확인합니다. (보통 500ms~1000ms 이내가 이상적입니다.)

### 4. 실무 꿀팁: 환경 변수(Environment) 활용

매번 서버 주소나 로그인 토큰을 복사 붙여넣기 할 필요가 없습니다.

- **Variables 설정**: {{base_url}}이나 {{auth_token}}처럼 변수를 만들어두면, '개발 서버'와 '스테이징 서버'를 클릭 한 번으로 전환하며 테스트할 수 있습니다.

### 5. 실무 보고 예시 (API 결함 보고)

#### [결함 요약] 
장바구니 담기 API 수량 유효성 검사 미흡

#### [테스트 API] 
POST /api/v1/cart

#### [테스트 데이터] 
```json
{ 
  "item_id": 505, 
  "quantity": -1 
}
```

#### [현상 확인] 
수량에 음수(-1)를 입력했음에도 `200 OK` 응답과 함께 장바구니에 상품이 담김.

#### [의견] 
백엔드 단에서 수량에 대한 양수 체크 로직이 누락된 것으로 보입니다.

## 💡 테스터를 위한 한 줄 전략

"화면 뒤의 진실을 보세요." UI는 예쁘게 보일지라도 API가 엉성하면 시스템은 언제든 무너질 수 있습니다. Postman을 통해 서버의 심장을 직접 두드려보는 테스터가 되세요.