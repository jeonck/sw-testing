# 웹 테스터의 필수 무기: 크롬 개발자 도구(F12)로 네트워크 에러 잡아내기

웹 테스터에게 크롬 개발자 도구의 [Network] 탭은 블랙박스 내부를 들여다보는 '엑스레이'와 같습니다. 화면에서 "에러가 발생했습니다"라는 단순한 메시지만 뜰 때, 개발자에게 **"API 응답이 500으로 오네요"**라고 말할 수 있는 테스터가 되기 위한 실무 가이드를 정리해 드립니다.

## 🛠️ 크롬 네트워크 탭, 실무 활용 매뉴얼

### 1. 네트워크 에러의 첫 단추: 상태 코드(Status Code) 확인

가장 먼저 봐야 할 곳은 Status 열입니다. 숫자의 앞자리만 봐도 범인을 찾을 수 있습니다.

- **2xx (Success)**: 일단 서버까지는 잘 갔다 온 것입니다. 화면이 안 나온다면 프론트엔드 로직 문제일 확률이 높습니다.

- **4xx (Client Error)**: 테스터가 보낸 데이터가 잘못되었거나(400), 권한이 없거나(401/403), 주소를 잘못 찾은(404) 경우입니다. 기획서와 입력값을 대조해 보세요.

- **5xx (Server Error)**: 서버 내부에서 터진 문제입니다. 이때는 Response 탭에 찍힌 에러 메시지를 캡처해서 개발자에게 전달해야 합니다.

### 2. "데이터가 왜 이래?" - Payload와 Response 분석

화면에 엉뚱한 값이 노출될 때 사용하는 방법입니다.

- **Payload (기존 Headers 내)**: 내가 서버로 보낸 데이터입니다. "나는 10,000원을 보냈는데 왜 에러가 나지?" 하고 확인해 보면, 실제로는 10000이 아닌 null이나 공백이 전송되고 있을 때가 많습니다.

- **Response**: 서버가 나에게 돌려준 데이터입니다. 기획서에는 '성공'이라고 되어 있는데 응답 값에 errorCode: "USER_NOT_FOUND" 등이 찍혀 있다면 기획과 구현의 불일치를 잡아낸 것입니다.

### 3. "너무 느려요" - 가속도와 병목 구간 체크

성능 테스팅의 기초입니다.

- **Waterfall 차트**: 어떤 리소스(이미지, 스크립트 등)가 가장 오래 걸리는지 시각적으로 보여줍니다.

- **Throttling (속도 제어)**: 우측 상단의 'No throttling'을 클릭해 **'Fast 3G'나 'Slow 3G'**로 바꿔보세요. 지하철이나 엘리베이터 같은 불안정한 네트워크 환경에서 앱이 무한 로딩에 빠지진 않는지 테스트할 수 있습니다.

### 4. 실무자만 아는 꿀팁: Preserve Log와 Disable Cache

- **Preserve Log**: 페이지가 이동(Redirect)되면 이전 로그가 사라지는데, 이를 체크해두면 페이지 전환 과정에서 발생하는 에러 로그를 끝까지 추적할 수 있습니다.

- **Disable Cache**: 브라우저에 저장된 옛날 데이터 때문에 버그가 안 나타날 수 있습니다. 이를 체크하고 테스트해야 **'항상 최신 코드'**를 기준으로 검증할 수 있습니다.

## 📝 개발자가 좋아하는 결함 보고서 예시 (네트워크 편)

### [결함 요약] 
마이페이지 진입 시 '데이터 조회 실패' 팝업 발생

### [재현 경로] 
로그인 -> 마이페이지 아이콘 클릭

### [네트워크 확인 결과]

- Request URL: /api/v1/user/profile
- Status Code: 500 Internal Server Error
- Response Body: {"message": "Database Connection Timeout"}

### [의견] 
서버 DB 응답 지연으로 인한 에러로 보입니다. 서버 로그 확인 부탁드립니다.